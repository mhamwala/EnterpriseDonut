<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.tees.q5113445live.enterpriseproject2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".login_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".sign_up_user" />
        <activity android:name=".sign_up_driver" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RequestCourier" />
        <activity android:name=".UserDetailsActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".DriverActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".VehicleDetailsActivity" />

        <activity android:name=".AllAvaliableJobsActivity" />
        <activity
            android:name=".NavigationDrawer"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

    </application>

</manifest>